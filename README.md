# replica-node-sqlite-js
A lightweight, full-stack web replica that dynamically renders user profiles based on URL query parameters. Built with a pure Vanilla JS frontend that fetches data from a Node.js REST API backed by a SQLite database. No heavy frameworks, just clean web standards.
